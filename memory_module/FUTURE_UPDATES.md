# Memory Module Future Updates

- Add memory delete command: `[memory][delete][memory_id]`.
- Add memory update command: `[memory][update][memory_id] new text`.
- Add importance levels such as normal, important, and critical.
- Add AMADEUS suggestions: “This may be worth saving to memory.”
- Add module-scoped memory: `[memory][module][identity_module] ...`.
- Add search/filter in the Memory panel.
- Add memory export and backup tools.
- Add memory conflict detection and cleanup review.
- Add explicit source-registration adapters for project files, dedicated chats, Flow Chat, exports, materials, and sheets through their public module APIs only.
- Add source-specific registration adapters for owners that can safely expose their content through public APIs.
- Add an optional disposable local vector adapter after structured retrieval quality is validated.

## Mind Map synchronization follow-ups

- Add a dedicated Memory panel editor and confirmation flow for update/delete operations now available through the service boundary.
