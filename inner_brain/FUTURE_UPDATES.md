# Inner Brain Future Updates

- Add model-health presentation only through explicit UI actions.
- Keep model write candidates limited to fixed registered pending-action kinds; never expand them into paths, shell commands, or direct persistence.
- Consider richer advisory metadata intent only after it retains Core validation, fixed document kinds, and general-chat-only routing.

## Core ownership cleanup — 2026-09-12

- Ownership direction: Keep analysis advisory when future capabilities are added.
