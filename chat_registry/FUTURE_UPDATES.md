# Chat Registry Future Updates

- Add relevance filtering for Flow requests while keeping the metadata-only API.
- Add optional safe metadata ranking when dedicated-chat counts grow.
- Keep summaries and deep message retrieval as separate explicit context layers.
- Design scope-aware retrieval only as an explicit, permission-protected feature.
