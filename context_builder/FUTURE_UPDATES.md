# Context Builder Future Updates

- Add message importance weighting.
- Add per-chat summaries when history becomes too long.
- Add direct message references such as `[current][5]`.
- Add module-specific context rules for reasoning profiles.
- Add token-aware trimming instead of character-based trimming.
